{
  "name": "sylvia-browser-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:backend": "npm run dev -w sylvia-backend",
    "build:core": "npm run build -w @sylvia/core",
    "build:backend": "npm run build -w sylvia-backend"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
